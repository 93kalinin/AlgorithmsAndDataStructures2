/**
 * Binomial-heap-based implementation of a priority queue.
 * Null values are not allowed.
 */
public class PriorityQueue<T extends Comparable<T>> {

    private final SortedList<T> forest = new SortedList<>();

    private static class BinomialNode<T> {

    }

}